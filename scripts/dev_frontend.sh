#!/bin/bash

# The frontend is served by the backend itself, so this script is a
# convenience stub.  It simply informs the developer to use
# ``dev_all.sh``.

echo "The MIND frontend is served by the backend. Use scripts/dev_all.sh to start both."