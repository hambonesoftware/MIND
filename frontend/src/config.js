// Configuration for the frontend.  API_BASE resolves relative to the
// current origin so that the same code works in development and
// production.  If you serve the backend on a different host/port you
// can set this value accordingly.

export const API_BASE = "";
export const USE_NODE_GRAPH = false;
