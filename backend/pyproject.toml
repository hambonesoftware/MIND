[project]
name = "mind-api"
version = "0.0.0"
description = "MIND backend skeleton"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
  "pydantic>=2.0",
]

[tool.uvicorn]
